<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.app_body_mass_index">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Appbodymassindex">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Afterverif"></activity>
        <activity android:name=".Acour"></activity>
        <activity android:name=".Gestionimc"></activity>
        <activity android:name=".ListeCours"></activity>
        <activity android:name=".Update"></activity>
        <activity android:name=".ListeImc"></activity>
        <activity android:name=".Courbe"></activity>

        <service android:name=".AlarmService" android:enabled="true"/>

        <receiver android:name=".AlarmReceiver"/>
    </application>

</manifest>